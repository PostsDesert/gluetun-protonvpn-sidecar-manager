# -----------------------------------------------------------------------------
# ProtonVPN Manager Configuration
# -----------------------------------------------------------------------------
# Your main Proton account credentials (for authentication & API access)
PROTON_USERNAME=your_proton_username
PROTON_PASSWORD=your_proton_password

# Comma-separated list of target cities (e.g., "San Jose,New York")
TARGET_CITIES=San Jose

# Target Country Code (e.g., US). Leave blank for no country filtering.
TARGET_COUNTRY=US

# Monitoring Intervals (Seconds)
# How often to check connectivity (ping)
HEALTH_CHECK_INTERVAL=60
# How often to check for better servers (load balancing)
LOAD_CHECK_INTERVAL=900

# -----------------------------------------------------------------------------
# WireGuard Static Config (From Proton Dashboard)
# -----------------------------------------------------------------------------
# Generate a WireGuard config for "Router" in ProtonVPN dashboard to get these.
# This Private Key is for the WireGuard interface itself.
WIREGUARD_PRIVATE_KEY=your_private_key_here
WIREGUARD_ADDRESSES=10.2.0.2/32

# -----------------------------------------------------------------------------
# Tailscale Config
# -----------------------------------------------------------------------------
# Reusable Pre-Auth Key from Headscale/Tailscale
TS_AUTHKEY=tskey-auth-xxxxxx
# Login server URL (if using Headscale)
TS_LOGIN_SERVER=https://headscale.example.com

# -----------------------------------------------------------------------------
# Multi-Instance Configuration
# -----------------------------------------------------------------------------
# Unique name for this VPN instance (e.g. "us", "jp").
# This prefix is used for container names: e.g. "us-gluetun", "us-exit-node"
VPN_INSTANCE_NAME=proton

# Project name for Docker Compose isolation (should match directory name or be unique)
COMPOSE_PROJECT_NAME=tailscale-proton

# Port for the Network Anchor (Health check endpoint)
# Leave blank (or set to 0) to let Docker assign a random port
ANCHOR_PORT=0

# Name of this environment file (so the manager knows which file to update)
ENV_FILE_NAME=.env

# -----------------------------------------------------------------------------
# Dynamic Variables (Managed by Sidecar - Leave Empty)
# -----------------------------------------------------------------------------
# The manager script will populate these automatically based on the best server found.
PROTON_SERVER_NAME=
WIREGUARD_ENDPOINT_IP=
WIREGUARD_ENDPOINT_PORT=
WIREGUARD_PUBLIC_KEY=
